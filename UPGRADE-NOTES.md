# Connect Three — تحديث الشكل والحركة

## اللي اتغيّر

**ملفين جداد بس، وباقي المشروع زي ما هو:**
- `js/vendor3d.min.js` — محرك الرسوميات (Three.js) مضغوط جوه المشروع، مش من الإنترنت
- `js/experience.js` — طبقة الحركة والـ3D، بتشتغل لوحدها في كل صفحة

**اتعدّل:**
- `css/style.css` — نظام التصميم الجديد (النسخة القديمة محفوظة في `css/style.OLD.backup.css`)
- الـ25 صفحة HTML: اتضاف سطرين script في آخر كل صفحة، واتحوّلت 80 لون مكتوب يدوي لمتغيرات عشان الوضع النهاري يشتغل

**مااتغيّرش خالص:** كل ملفات الـ JS بتاعة الشغل (auth, supabaseclient, admin, realtime, badges...)، قاعدة البيانات، وكل أسماء الكلاسات. يعني منطق التطبيق زي ما هو بالظبط.

## اللي بقى في التطبيق

- شاشة تحميل بعداد — **أول مرة بس** في الجلسة، مش مع كل صفحة
- انتقال بستارة بين الصفحات
- مؤشر ماوس مخصص (على الكمبيوتر بس)
- 3D: هالة حوالين اللوجو في صفحة الدخول، شكل بيلف جنب اسم التطبيق، أشكال خلفية، وشكل خلف أرقام النتيجة
- ظهور الكروت بحركة مع السكرول + شريط تقدم فوق
- الأرقام بتعد لوحدها في لوحة النتيجة
- تبديل ليلي/نهاري (بيتحفظ) + أصوات نقر خفيفة

## حاجات مهمة تعرفها

1. **الأداء بيتظبط لوحده** — التطبيق بيقيس سرعة الجهاز بعد ثانيتين، ولو ضعيف بيقلل الرسوميات، ولو ضعيف جدًا بيقفل الـ3D خالص ويسيب التطبيق شغال عادي. مهم لموبايلات المؤتمر المختلفة.

2. **المحتوى مستحيل يختفي** — فيه أمان: أي كارت مايظهرش خلال 8 ثواني بيتعرض غصب. عشان الحركة عمرها ما تخفي بيانات حقيقية لو النت فصل أو Supabase اتأخر.

3. **الملفات بتتحمل مرة واحدة** — محرك الرسوميات 650 كيلوبايت، بس المتصفح بيحفظه في الكاش بعد أول صفحة، فالتنقل بين الصفحات مابيعيدش تحميله.

4. **لو حد مفعّل "تقليل الحركة"** في إعدادات موبايله، كل الحركات والـ3D بيتقفلوا تلقائيًا.

5. **للتجربة:** ضيف `?gfx=high` في آخر أي رابط عشان توقف تخفيض الجودة التلقائي وتشوف الـ3D بأعلى جودة.

## لو حبيت ترجع للشكل القديم

```
cp css/style.OLD.backup.css css/style.css
```
وامسح سطرين الـ script من آخر الصفحات (أو سيبهم، مش هيأثروا من غير الـ CSS).
